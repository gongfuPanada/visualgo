VISUALGO

For installation use maven:

Read-only address
$ git clone git://github.com/jhilary/visualgo.git

With authentification:
$ git clone git@github.com:jhilary/visualgo.git

$ cd visualgo/trunk
$ mvn package

Import in eclipse:
File -> Import -> Existing Maven Project

Build in eclipse:
Run as -> Maven build.. -> [set goal "package"]

Run in Eclipse:
Run as -> Java Application -> [choose Main]

For run web application:
$ mvn jetty:run

http://localhost:8080/visualgo/main/mincut/algos.do

